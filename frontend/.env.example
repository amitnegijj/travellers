# The Express API's origin. In dev, `npm run dev` runs it on :4000.
VITE_API_URL=http://localhost:4000

# Optional: point at a vector style (MapTiler / Protomaps) to override the
# built-in CARTO raster basemap. Leave unset for the no-API-key default.
# VITE_MAP_STYLE_URL=
